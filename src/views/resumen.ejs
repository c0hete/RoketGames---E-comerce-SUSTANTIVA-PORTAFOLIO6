<%- include('header.ejs', { pageTitle: 'RocketGames' }) %>
<div class="container" id="resumen">
  <div class="row">
    <div class="col-md-8 text-light">
      <h1 class="modal-title d-flex align-items-center"><%= producto.nombre %>
        <button class="btn btn-secondary ms-3" onclick="window.history.back()">Volver</button>
      </h1>
      <div class="modal-body">
        <p>Descripción:</p>
        <p><%= producto.descripcion %></p>
        <p>Resumen:</p>
        <p><%= producto.resumen %></p>
        <h2>Precio: $<%= producto.precio.toFixed(2) %></h2>
      </div>
    </div>
    <div class="col-md-4">
      <img src="<%= producto.imagen %>" alt="<%= producto.nombre %>" class="img-fluid">
    </div>
  </div>
</div>
<%- include('footer.ejs') %>

